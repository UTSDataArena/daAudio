class ChannelConfig(object):
    """
    A pre-defined audio configuration to simplify sound setup in a visualisation.
    """

    def __init__(self):
        super(ChannelConfig, self).__init__()

        self.sources = []
