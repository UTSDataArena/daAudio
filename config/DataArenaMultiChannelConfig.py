from daAudio.config.ChannelConfig import ChannelConfig
from daAudio.openal.audio import SoundSource


class DataArenaMultiChannelConfig(ChannelConfig):

    # An audio configuration which represents the full 16-channel audio setup within the
    # production Data Arena.

    def __init__():
        super(DataArenaMultiChannelConfig, self).__init__()
