# OMEGALIB - daAudio module

The daAudio library contains re-usable code modules to support the implementation of 3D positional sound effects within the Data Arena. It relies on *omegalib* and *OpenAL*.
